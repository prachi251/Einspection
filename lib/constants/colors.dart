import 'package:flutter/material.dart';

const Color primaryBlue = Color.fromARGB(255, 30, 66, 229);     // Matches login blue
const Color primaryOrange = Color(0xFFF57C00);   // Matches login orange
const Color backgroundWhite = Color(0xFFFFFFFF); // Clean white background
const Color accentOrange = Color.fromARGB(255, 255, 123, 0);
